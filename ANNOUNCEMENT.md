# 📢 宿舍电量查询功能上线公告

再也不用手动开小程序看电量了！现在机器人直接在群里帮你查，还能每天定时播报、低电量自动提醒。

## ✨ 能干什么

- ⚡ 群里一句话查宿舍剩余电量、今日用电
- 🏠 支持多房间（照明/空调分开的也能一起查）
- ⏰ 每天 8 点、20 点自动播报（可订阅/退订）
- ⚠️ 电量低于阈值自动提醒充值
- 👥 每个人绑自己的账号，各查各的宿舍，互不干扰

## 🚀 三步上手

**第 1 步：绑定学校代码和账号**

```
/电量绑定 <学校代码> <账号>
```

例：`/电量绑定 1000145 你的账号`

> 账号说明：填你在完美校园里的 outid，**一般是身份证号**（不是学号！用学号会查不到人）。

**第 2 步：添加房间**

```
/电量房间          ← 自动列出你账号绑定的房间，带序号
/电量添加          ← 不带数字，全部加入监控
/电量添加 1 2      ← 或者只加指定序号的房间
/电量添加 1020     ← 懒得看序号？直接写房间号也行
```

**第 3 步：查询 & 订阅**

```
/电量              ← 立即查询
/电量推送          ← 订阅每日 8 点、20 点自动播报（在哪个群发就推哪个群）
```

**完整示例**（以广东科技学院为例）：

```
/电量绑定 1000145 你的账号
/电量房间
   → 1. 主分区_23栋X_9层_901  当前 66 度
   → 2. 主分区_23栋X_10层_1002  当前 40 度
/电量添加
   → ✅ 已添加：主分区_23栋X_9层_901、主分区_23栋X_10层_1002
/电量
   → 🏠主分区_23栋X_9层_901｜剩余66度｜今日12度｜近7日 一11 二13 …｜上月35度｜累购2453.9度
   → 🏠主分区_23栋X_10层_1002｜剩余40度｜…
/电量推送
   → ✅ 每天 8 点、20 点自动播报到本群
```

> 小技巧：指令不用打斜杠、也可以不空格，`电量删除1`、`电量绑定1000145 你的账号` 都能识别；删除/添加还支持房间号关键字，比如 `电量删除 927`。

其他：`/电量删除 <序号或房间号>` 删房间、`/电量解绑` 清除自己的数据、`/电量搜校 <账号>` 自动找学校代码。

## 🏫 不知道自己学校的代码怎么办？

每所学校在完美校园里有一个数字代码（customercode），比如广东科技学院是 `1000145`。获取方式按推荐顺序：

### 方式一：机器人自动搜（推荐，零门槛）

```
/电量搜校 你的账号
```

机器人会在后台扫描所有已知编码段（已限速防风控：默认 6 请求/秒，全量约 35 分钟；会先试历史命中过的学校代码，多数情况几十秒就有结果）。扫完自动把结果发给你。如果扫出多个，逐个 `/电量绑定` 后用 `/电量房间` 验证——能列出你宿舍的就是对的。

### 方式二：抓包（100% 准确）

1. 电脑开个热点（或同一 Wi-Fi），手机连上
2. 电脑装 Fiddler Classic，端口 8888，勾选 Allow remote computers + Decrypt HTTPS
3. 手机 Wi-Fi 手动代理指向电脑 IP:8888，浏览器打开 `http://电脑IP:8888` 下载并安装证书
4. 手机进支付宝 → 完美校园 → 电量页
5. 在 Fiddler 里找发往 `xqh5.17wanxiao.com` 的请求，URL 里的 `customercode=数字` 就是学校代码；请求里 `roomverify` 是房间凭证（本插件其实不需要手填 roomverify，`/电量房间` 会自动列出）

### 方式三：问管理员

让已经配好的同学帮你查一下。

## ❓ 常见问题

- **提示"人员信息不存在"** → 你用学号了，换成身份证号
- **`/电量房间` 说没有绑定房间** → 先去支付宝小程序「完美校园」里把宿舍房间绑定一遍，再回来执行
- **查出来的电量不对/为 0** → 确认绑定的房间序号选对了（照明和空调是两个房间/电表）
- **隐私**：你的账号和房间数据只存在机器人服务器本地，仅用于查询电量，`/电量解绑` 可随时彻底删除

有问题群里 @管理员，附上报错截图即可。
